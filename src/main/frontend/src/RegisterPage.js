// RegistrPage.js
import React from 'react';

const RegisterPage = ({ user }) => (
    <div>
        <h2>Welcome, {user}!</h2>
        <p>This is the membership page. More features can be added here.</p>
    </div>
);

export default RegisterPage;
